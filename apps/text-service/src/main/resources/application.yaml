server:
  servlet:
    context-path: /
  error:
    whitelabel:
      enabled: false
  forward-headers-strategy: framework
  port: 8080

spring:
  application:
    name: text-service

  datasource:
    url: jdbc:mariadb://localhost:3306/UnicodeBD
    username: UUnicodeBD
    password: PUnicodeBD2026
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update

normalizer:
  service:
    url: http://localhost:8081

logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger - %kvp%n"

  level:
    root: INFO
    br.com.unicode: INFO
    org.springframework.web: WARN
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE