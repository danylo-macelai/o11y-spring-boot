server:
  servlet:
    context-path: /
  error:
    whitelabel:
      enabled: false
  forward-headers-strategy: framework
  port: 8081

spring:
  application:
    name: normalizer-service

  datasource:
    url: jdbc:mariadb://localhost:3306/UnicodeBD
    username: UUnicodeBD
    password: PUnicodeBD2026
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update